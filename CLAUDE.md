# Simple B-rep implementation

This project will implement:

- simple B-rep data structure and an operation, the same what the SketchUp application calls push-pull, in a static library
- functions to load and save the B-rep into STEP
- corresponding tests
